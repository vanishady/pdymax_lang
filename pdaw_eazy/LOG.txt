Modifiche apportate nell'ultima versione:

- algoritmo di posizionamento con graphviz: è molto lento, usa sottoprocesso. 
(provvisorio)

- frontend verso json: utilizza il file 'utils/correspondances.json' per tradurre il tipo di nodo
(file json da ampliare) e quando non esiste una traduzione diretta usa oggetti creati da me, Z.B. 'spigot_object',
presenti nella cartella 'outputs'
alternativa: si potrebbero aggiungere i nodi direttamente nella memoria. E' più lenta da scrivere, ma sarebbe la soluzione
più adeguata

- frontend verso pd: non utilizza il file correspondances perché presuppone l'installazione della libreria Cyclone per
la compatibilità da max verso pd

- controllo dei nodetypes: commentato (non funziona)






